====================================================Homework 3 README==================================================
        Date:   March 12, 2020
        Author: David Richards
        Class:  CS4760
=======================================================================================================================
Invocation: ./master [-h] [-n x]
=======================================================================================================================
How to Run:
                1. Type make
                2. Run the executable by typing ./oss [-h] [-n x-s x -b B -i I | -o filename]
                3. ./master alone will run with the following default ./master -n 64
                4. -h   = describe how the project should be run and then terminate
                   -n x = Number of random integers [0,256) to print to input file named (intFile) (Default 64)
		5. The child processes will update file (adder_log) to tell you what has been done, including
		   computation results
                6. Type make clean to remove (intFile, adder_log, master, bin_adder, master.o and bin_adder.o)
=======================================================================================================================
Version Control (logging technique used)
                1. My log files are included in /classes/OS/drichard/drichard.3/log.txt
=======================================================================================================================
1. All project requirments met
2. Makefile follows proper suffix rules
3. Currently no known issues
=======================================================================================================================
TODO:
	Run for max 5
	Comments
	fix PIDs
	add second computation
	change clock?                                                                                                                       ~         
